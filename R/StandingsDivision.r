# NHL API
#
# Documenting the publicly accessible portions of the NHL API.
#
# OpenAPI spec version: 1.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' StandingsDivision Class
#'
#' @field id 
#' @field name 
#' @field link 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
StandingsDivision <- R6::R6Class(
  'StandingsDivision',
  public = list(
    id = NULL,
    name = NULL,
    link = NULL,
    initialize = function(id, name, link){
      if (!missing(id)) {
        stopifnot(is.numeric(id), length(id) == 1)
        stopifnot(R6::is.R6(id))
        self$id <- id
      }
      if (!missing(name)) {
        stopifnot(is.character(name), length(name) == 1)
        self$name <- name
      }
      if (!missing(link)) {
        stopifnot(is.character(link), length(link) == 1)
        self$link <- link
      }
    },
    toJSON = function() {
      StandingsDivisionObject <- list()
      if (!is.null(self$id)) {
        StandingsDivisionObject[['id']] <- self$id$toJSON()
      }
      if (!is.null(self$name)) {
        StandingsDivisionObject[['name']] <- self$name
      }
      if (!is.null(self$link)) {
        StandingsDivisionObject[['link']] <- self$link
      }

      StandingsDivisionObject
    },
    fromJSON = function(StandingsDivisionJson) {
      StandingsDivisionObject <- jsonlite::fromJSON(StandingsDivisionJson)
      if (!is.null(StandingsDivisionObject$id)) {
        idObject <- BigDecimal$new()
        idObject$fromJSON(jsonlite::toJSON(StandingsDivisionObject$id, auto_unbox = TRUE))
        self$id <- idObject
      }
      if (!is.null(StandingsDivisionObject$name)) {
        self$name <- StandingsDivisionObject$name
      }
      if (!is.null(StandingsDivisionObject$link)) {
        self$link <- StandingsDivisionObject$link
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "id": %s,
           "name": %s,
           "link": %s
        }',
        self$id$toJSON(),
        self$name,
        self$link
      )
    },
    fromJSONString = function(StandingsDivisionJson) {
      StandingsDivisionObject <- jsonlite::fromJSON(StandingsDivisionJson)
      BigDecimalObject <- BigDecimal$new()
      self$id <- BigDecimalObject$fromJSON(jsonlite::toJSON(StandingsDivisionObject$id, auto_unbox = TRUE))
      self$name <- StandingsDivisionObject$name
      self$link <- StandingsDivisionObject$link
    }
  )
)
